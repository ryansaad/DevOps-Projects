pipeline {
    agent any
    
    tools {
        jdk "jdk17"
        maven "maven3"
    }
    
    environment {
        // Points to the deep nested folder
        PROJECT_DIR = "DevOps-Project-29/full-stack-blogging-app" 
        DOCKER_IMAGE = "ryansaad85/blogging-app"
        SCANNER_HOME = tool 'sonar-scanner'
    }
    
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Compile') {
            steps {
                dir("${PROJECT_DIR}") {
                    sh "mvn compile"
                }
            }
        }
        
        stage('Trivy FS Scan') {
            steps {
                dir("${PROJECT_DIR}") {
                    sh "trivy fs . --format table -o fs.html"
                }
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                dir("${PROJECT_DIR}") {
                    withSonarQubeEnv('sonar-server') {
                        sh '''$SCANNER_HOME/bin/sonar-scanner \
                        -Dsonar.projectName=Blogging-app \
                        -Dsonar.projectKey=Blogging-app \
                        -Dsonar.java.binaries=target'''
                    }
                }
            }
        }
        
        stage('Build') {
            steps {
                dir("${PROJECT_DIR}") {
                    sh "mvn package"
                }
            }
        }
        
        stage('Publish Artifacts') {
            steps {
                dir("${PROJECT_DIR}") {
                    withMaven(globalMavenSettingsConfig: 'maven-settings', jdk: 'jdk17', maven: 'maven3', mavenSettingsConfig: '', traceability: true) {
                        sh "mvn deploy"
                    }
                }
            }
        }
        
        stage('Docker Build & Tag') {
            steps {
                dir("${PROJECT_DIR}") {
                    script {
                        withDockerRegistry(credentialsId: 'docker', url: 'https://index.docker.io/v1/') {
                            // The '.' refers to the current directory, which is now the correct subfolder
                            sh "docker build -t ${DOCKER_IMAGE}:${BUILD_NUMBER} ."
                            sh "docker tag ${DOCKER_IMAGE}:${BUILD_NUMBER} ${DOCKER_IMAGE}:latest"
                        }
                    }
                }
            }
        }
        
        stage('Trivy Image Scan') {
            steps {
                // No dir() needed here because Trivy scans the docker engine, not a file path
                sh "trivy image --format table -o image.html ${DOCKER_IMAGE}:latest"
            }
        }
        
        stage('Docker Push Image') {
            steps {
                script {
                    withDockerRegistry(credentialsId: 'docker', url: 'https://index.docker.io/v1/') {
                        sh "docker push ${DOCKER_IMAGE}:${BUILD_NUMBER}"
                        sh "docker push ${DOCKER_IMAGE}:latest"
                    }
                }
            }
        }
    }
    
    post {
        always {
            script {
                def jobName = env.JOB_NAME
                def buildNumber = env.BUILD_NUMBER
                def pipelineStatus = currentBuild.result ?: 'SUCCESS'
                echo "Pipeline finished with status: ${pipelineStatus}"
            }
        }
    }
}